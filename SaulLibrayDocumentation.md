📄 Документация к библиотеке Saul Goodman UI Framework

📋 Содержание

1. Базовое использование
2. Создание окна
3. Табы и секции
4. Элементы управления
5. Вспомогательные функции
6. Система тем
7. API Reference

---

🚀 Базовое использование

Подключение библиотеки

```lua
local Library = loadstring(game:HttpGet("URL_БИБЛИОТЕКИ"))()
```

Минимальный пример

```lua
local Window = Library:Window({
    Title = "Мой интерфейс",
    Theme = "Dark"
})

local Tab = Window:Tab({
    Title = "Главная",
    Icon = "house"
})

Tab:Label({
    Title = "Приветствие",
    Desc = "Добро пожаловать!"
})
```

---

🖼️ Создание окна

Параметры Window

```lua
Library:Window({
    Title = "Заголовок",           -- Текст в заголовке
    Desc = "Описание",             -- Подзаголовок (опционально)
    Icon = "icon-name",           -- Название иконки (или Asset ID)
    Theme = "Dark",               -- Название темы

    Config = {
        Keybind = Enum.KeyCode.LeftControl,  -- Клавиша показа/скрытия
        Size = UDim2.new(0, 550, 0, 350)     -- Размер окна
    },

    CloseUIButton = {
        Enabled = true,            -- Кнопка скрытия окна
        Text = "СКРЫТЬ/ПОКАЗАТЬ"   -- Текст на кнопке
    }
})
```

Доступные темы

· "Dark" (по умолчанию)
· "Amethyst", "Sapphire", "Amber", "Topaz"
· "Ocean", "Solarized", "Emerald", "Midnight"
· "RoseQuartz", "Cyberpunk", "Desert", "Frost"
· "Galaxy", "Crimson", "Halloween", "NewYear"

---

📑 Табы и секции

Создание таба

```lua
local Tab = Window:Tab({
    Title = "Название таба",
    Icon = "icon-name"  -- Иконка из библиотеки иконок
})
```

Добавление секции

```lua
Tab:Section({
    Title = "Название секции"
})
```

Линия-разделитель (только в табах)

```lua
Tabs:Line()  -- Создает горизонтальную линию
```

---

🎛️ Элементы управления

1. Label (Текстовая метка)

```lua
Tab:Label({
    Title = "Заголовок",
    Desc = "Описание"  -- Опционально
})
```

Доступные методы:

```lua
local label = Tab:Label({...})
label:SetTitle("Новый заголовок")
label:SetDesc("Новое описание")
label:SetVisible(true/false)
```

2. Toggle (Переключатель)

```lua
Tab:Toggle({
    Title = "Название",
    Desc = "Описание",      -- Опционально
    Image = "icon-name",    -- Опционально
    Value = false,          -- Начальное значение
    Callback = function(value)
        print("Значение:", value)
    end
})
```

Доступные методы:

```lua
local toggle = Tab:Toggle({...})
toggle:SetTitle("Новое название")
toggle:SetDesc("Новое описание")
toggle:SetVisible(true/false)
toggle:SetValue(true/false)  -- Установить значение
```

3. Button (Кнопка)

```lua
Tab:Button({
    Title = "Название кнопки",
    Desc = "Описание",      -- Опционально
    Image = "icon-name",    -- Опционально
    Callback = function()
        print("Кнопка нажата!")
    end
})
```

4. Slider (Слайдер)

```lua
Tab:Slider({
    Title = "Название",
    Desc = "Описание",      -- Опционально
    Image = "icon-name",    -- Опционально
    Min = 0,                -- Минимальное значение
    Max = 100,              -- Максимальное значение
    Rounding = 0,           -- Округление (кол-во знаков)
    Value = 50,             -- Начальное значение
    Callback = function(value)
        print("Значение:", value)
    end
})
```

Доступные методы:

```lua
local slider = Tab:Slider({...})
slider:SetTitle("Новое название")
slider:SetDesc("Новое описание")
slider:SetVisible(true/false)
slider:SetValue(75)         -- Установить значение
slider:SetMin(0)            -- Изменить минимум
slider:SetMax(200)          -- Изменить максимум
```

5. Dropdown (Выпадающий список)

```lua
Tab:Dropdown({
    Title = "Название",
    Desc = "Описание",      -- Опционально
    Image = "icon-name",    -- Опционально
    List = {"Опция 1", "Опция 2", "Опция 3"},  -- Список
    Value = "Опция 1",      -- Значение по умолчанию
    Multi = false,          -- Множественный выбор
    Callback = function(value)
        print("Выбрано:", value)
    end
})
```

Доступные методы:

```lua
local dropdown = Tab:Dropdown({...})
dropdown:SetTitle("Новое название")
dropdown:SetDesc("Новое описание")
dropdown:SetVisible(true/false)
dropdown:SetValue("Новая опция")  -- Установить значение
dropdown:Add("Новая опция")       -- Добавить в список
dropdown:Clear()                  -- Очистить список
dropdown:Clear("Конкретная опция") -- Удалить конкретную
```

6. Textbox (Текстовое поле)

```lua
Tab:Textbox({
    Title = "Название",
    Desc = "Описание",            -- Опционально
    Image = "icon-name",          -- Опционально
    Value = "",                   -- Начальный текст
    Placeholder = "Введите текст",-- Текст-подсказка
    ClearText = false,            -- Очищать при фокусе
    Callback = function(text)
        print("Введен текст:", text)
    end
})
```

Доступные методы:

```lua
local textbox = Tab:Textbox({...})
textbox:SetTitle("Новое название")
textbox:SetDesc("Новое описание")
textbox:SetVisible(true/false)
textbox:SetValue("Новый текст")
textbox:SetPlaceholderText("Новая подсказка")
textbox:SetClearTextOnFocus(true/false)
```

7. Keybind (Привязка клавиши)

```lua
Tab:Keybind({
    Title = "Название",
    Desc = "Описание",      -- Опционально
    Image = "icon-name",    -- Опционально
    Key = Enum.KeyCode.E,   -- Клавиша по умолчанию
    Value = false,          -- Начальное состояние
    Callback = function(key, state)
        print("Клавиша:", key, "Состояние:", state)
    end
})
```

Доступные методы:

```lua
local keybind = Tab:Keybind({...})
keybind:SetTitle("Новое название")
keybind:SetDesc("Новое описание")
keybind:SetVisible(true/false)
keybind:SetValue(true/false)  -- Включить/выключить
keybind:SetKey(Enum.KeyCode.F) -- Изменить клавишу
```

8. Color Picker (Выбор цвета)

```lua
Tab:ColorPicker({
    Title = "Выбор цвета",
    Desc = "Описание",      -- Опционально
    Image = "icon-name",    -- Опционально
    Value = Color3.fromRGB(255, 255, 255),  -- Начальный цвет
    Callback = function(r, g, b)
        print("RGB:", r, g, b)
    end
})
```

Доступные методы:

```lua
local colorpicker = Tab:ColorPicker({...})
colorpicker:SetTitle("Новое название")
colorpicker:SetDesc("Новое описание")
colorpicker:SetVisible(true/false)
colorpicker:SetValue({R = 255, G = 0, B = 0})  -- Установить цвет
```

9. Code (Блок кода)

```lua
Tab:Code({
    Title = "Пример кода",
    Code = [[
        -- Ваш код здесь
        print("Hello World!")
    ]]
})
```

Доступные методы:

```lua
local code = Tab:Code({...})
code:SetTitle("Новое название")
code:SetCode("Новый код")  -- Установить новый код
```

---

🛠️ Вспомогательные функции

Уведомления (Notifications)

```lua
Window:Notify({
    Title = "Заголовок",
    Desc = "Описание уведомления",
    Time = 5  -- Время показа в секундах
})
```

Диалоговые окна (Dialogs)

```lua
Window:Dialog({
    Title = "Вопрос",
    Button1 = {
        Title = "Да",
        Color = Color3.fromRGB(0, 188, 0),
        Callback = function()
            print("Нажата кнопка Да")
        end
    },
    Button2 = {
        Title = "Нет",
        Color = Color3.fromRGB(226, 39, 6),
        Callback = function()
            print("Нажата кнопка Нет")
        end
    }
})
```

Иконки (Icons)

Библиотека использует встроенную систему иконок. Доступны:

· "house", "settings", "user", "bell"
· "zap", "eye", "code", "heart"
· "tree-pine", "snowflake", "gift"
· И многие другие (полный список в библиотеке)

Также можно использовать Asset ID:

```lua
Icon = 92155476988079  -- Числовой ID
Icon = "rbxassetid://92155476988079"  -- Строковый ID
```

---

🎨 Система тем

Смена темы

```lua
Library:setTheme("Dark")  -- Применяет тему Dark
```

Создание своей темы

```lua
local myTheme = {
    ['Shadow'] = Color3.fromRGB(15, 15, 15),
    ['Background'] = Color3.fromRGB(20, 20, 20),
    ['Page'] = Color3.fromRGB(18, 18, 18),
    ['Main'] = Color3.fromRGB(91, 68, 209),
    ['Text & Icon'] = Color3.fromRGB(255, 255, 255),

    ['Function'] = {
        ['Toggle'] = {
            ['Background'] = Color3.fromRGB(29, 28, 38),
            ['True'] = {
                ['Toggle Background'] = Color3.fromRGB(44, 34, 103),
                ['Toggle Value'] = Color3.fromRGB(91, 68, 209)
            },
            -- ... и так далее для всех элементов
        }
    }
}

-- Добавить тему в список
themes["MyTheme"] = myTheme

-- Применить
Library:setTheme("MyTheme")
```

Структура темы:

· Shadow - Тень окна
· Background - Фон окна
· Page - Фон страницы
· Main - Основной цвет (акценты)
· Text & Icon - Цвет текста и иконок
· Function.* - Цвета для каждого элемента

---

📚 API Reference

Методы Window

```lua
Window:Tab({...})         -- Создать таб
Window:Notify({...})      -- Показать уведомление
Window:Dialog({...})      -- Показать диалог
```

Методы Tabs

```lua
Tabs:Tab({...})           -- Создать таб (внутри Window)
Tabs:Line()              -- Линия-разделитель
Tabs:SelectTab(index)    -- Выбрать таб по индексу
```

Общие методы элементов

Большинство элементов имеют:

· :SetTitle(text) - Изменить заголовок
· :SetDesc(text) - Изменить описание
· :SetVisible(bool) - Показать/скрыть

---

💡 Советы и лучшие практики

Организация кода

```lua
-- 1. Создаем окно
local Window = Library:Window({...})

-- 2. Создаем табы по функциональности
local MainTab = Window:Tab({Title = "Главная"})
local VisualTab = Window:Tab({Title = "Визуал"})
local ScriptsTab = Window:Tab({Title = "Скрипты"})

-- 3. Группируем элементы по секциям
MainTab:Section({Title = "Персонаж"})
MainTab:Toggle({...})
MainTab:Slider({...})

MainTab:Section({Title = "Другое"})
MainTab:Button({...})
```

Обработка ошибок

```lua
Tab:Button({
    Title = "Тестовая кнопка",
    Callback = function()
        local success, result = pcall(function()
            -- Код который может сломаться
            riskyFunction()
        end)

        if not success then
            Window:Notify({
                Title = "Ошибка",
                Desc = result,
                Time = 5
            })
        end
    end
})
```

Производительность

· Используйте Value параметр для установки начальных значений
· Не создавайте элементы в циклах без необходимости
· Закрывайте неиспользуемые соединения (Connections)

---

🆘 Решение проблем

Ошибка: "Element is not a function"

Проверьте, что:

1. Библиотека загрузилась корректно
2. Вы используете правильный синтаксис
3. Все обязательные параметры указаны

Элементы не появляются

1. Проверьте Parent ScrollingFrame
2. Убедитесь что ScrollingFrame_1 существует
3. Проверьте размеры CanvasSize

Темы не применяются

1. Убедитесь что имя темы указано верно
2. Проверьте структуру темы
3. Убедитесь что SaveTheme инициализирован

---